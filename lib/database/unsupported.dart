import 'package:drift/drift.dart';

import '../InnerDatabase.dart';

InnerDatabase constructDb(String name, int dbVersion, MigrationStrategy migrationStrategy, {bool logStatements = false}) {
  throw 'Platform not supported';
}
